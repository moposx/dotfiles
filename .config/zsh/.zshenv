# Don't edit this file.
# It's been set read-only deliberately.
# Instead, use `.zshrc` (for interactive shell),
# and `.zprofile` (for login shell).

# Link it to `$HOME/.zshenv`.

# The `ZDOTDIR` is crucial for Zsh to locate configuration files.
export ZDOTDIR="$HOME/.config/zsh"
